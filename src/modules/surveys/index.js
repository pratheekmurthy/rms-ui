import React from 'react';
import RouteSwitch from 'src/components/RouteSwitch';

export default function index({ routes }) {
    return (
        <div>
            Survey Module! Components yet to be added
            <RouteSwitch routes={routes} />
        </div>
    );
}
